[
  import: [
  ],
  mappings: [
    "config.watch": [
      doc: "Watch for configuration changes and apply it directly",
      to: "exrm_reload.watch",
      datatype: :atom,
      default: false
    ]
  ],
  translations: [
  ]
]
